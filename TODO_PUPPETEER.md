# Puppeteer dropdown click task

- [ ] Create a standalone runner script to navigate to `https://pmsuryaghar.gov.in/#/` and click the dropdown trigger.
- [ ] Validate the selector works and click opens the expected listbox.
- [ ] If the selector is unstable, switch to an XPath/ancestor-based locator.
- [ ] Once validated, wire it into `script.js` (optional).